import { alpha, makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  list: {},
  items: {
    listStyle: "none",
  },
  item: {
    cursor: "pointer",
    marginBottom: 15
  },
}));
